name: Build and deploy

on: [push]

jobs:
#   azurelogin:
#     name: Azure Login
#     runs-on: ubuntu-latest
#     steps:
#     - name: Azure Login
#       uses: azure/login@v1
#       with:
#         creds: ${{ secrets.AZURE_CREDENTIALS }}
  theme1:
    # needs: azurelogin
    name: Theme 1
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-gitversion@v1
      with: 
        gitversion-version: 5.1.3
    - name: Check environment variables
      run: echo This branch is $GitVersion_BranchName